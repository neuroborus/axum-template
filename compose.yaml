services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      HTTP_HOST: 0.0.0.0
      HTTP_PORT: 3000
    restart: "no"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:3000/ops/health"]
      interval: 10s
      timeout: 3s
      retries: 3
